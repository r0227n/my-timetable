# 添付タイムテーブル画像の検証記録

2026-09-06、`HQzecSka0AAARG4.jpeg`（1500×2000）をChromeの実WebGPUで検証した。GLM-OCRとGemma 4 E2Bを実行し、モデルのモック、認識結果の手入力、出演者名の辞書置換は使用していない。

## 実装と比較結果

変更前の画像全体OCRでは、日付・会場・終演後物販の共通時間が欠落し、「物販」の誤読が発生した。Gemmaも表の列を取り違え、不正なJSONを返した。

最終版は画像から30行の境界を検出し、見出し1領域、3行ずつの表10領域、下部注記1領域を認識した。明確な表のセル対応はアプリで構造化し、Gemmaにはイベント情報だけを整形させた。小書き仮名の誤読は、元画像の文字境界・幅・高さが一致する場合だけ補正した。

## 通常フローでの全件照合

開発サーバーは `bun run dev --mode debug` で起動した。Viteの自動再読込が推論を中断するため、最終確認は `bun run build` 後の `bun run preview --port 5175`、`http://localhost:5175/my-timetable/` で実施した。

1. 通常の画像入力から添付画像をアップロードし、回転・切り抜き・手修正をせず解析を開始。
2. 確認画面に表示されたデータを、画像から別途転記した期待値と全件比較。
3. 出演者30組、ライブ30件、物販30件、全60件の開始・終了時刻、ブース26件、終演後物販4件が一致。
4. 開催日2026-09-13、会場KANDA SQUARE HALL、OPEN 09:15、START 09:30が一致。
5. 終演後物販4件は21:35〜22:55、相対表現「終演後」を保持し、出演行と下部注記の両方を元画像の根拠領域として保持。
6. 「こみっきゅおん!」の小書き仮名が一致。他の出演者名は補正で変更されていない。名称比較では全角・半角とアポストロフィの字形差を正規化した。
7. 全60件のIDが一意で、認識直後は全件未確認。全件を照合後に確認済みとし、予定選択へ遷移。
8. 「こみっきゅおん!」の物販だけを選択し、ライブは未選択になることを確認。INUWASIは出演者単位でライブ・物販の両方を選択。
9. 3件のタイムラインを生成し、SVGとICSを保存。ICSの15:30〜16:50、21:10〜21:35、21:35〜22:55、およびブースC・KANDA SQUARE HALLを確認。
10. localStorageは`ui.theme`と`ui.gemmaModel`のみ。画像・認識結果・予定の永続保存はない。

画像上の表記は「物販」のため、実画像では物販として検証した。「特典会」の種別、別会場とブースの保持、共通時刻のない「終演後」が未確定になることは自動テストで検証した。

## 品質ゲート

- `bun run format:check`: 成功
- `bun run lint`: 成功
- `bun run test`: 27ファイル・158件成功
- `bun run build`: 成功
- `git diff --check`: 成功

## ローカルの証拠

画像・生成データはリポジトリへ含めず、検証端末の一時ディレクトリだけに保存した。外部公開URLはない。

- 確認画面: `/tmp/timetable-review-final.png`
- 終演後物販の確認: `/tmp/timetable-after-show-final.png`
- 物販だけの選択: `/tmp/timetable-merch-only-final.png`
- タイムライン: `/tmp/timetable-timeline-final.png`
- ICS: `/tmp/timetable-final.ics`
- SVG: `/tmp/timetable-final.svg`
- 全件照合した認識直後のデータ: `/tmp/timetable-final-document.json`
- 独立した期待値による照合: `/tmp/verify-timetable.py`

本画像の受け入れ条件に未確認項目はない。別画像すべてに対するOCRの完全一致を保証するものではなく、認識結果の確認フローを維持する。データスキーマの破壊的変更はない。
